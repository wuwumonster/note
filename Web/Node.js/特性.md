## 大小写特性
对于toUpperCase(): 字符`"ı"`、`"ſ"` 经过toUpperCase处理后结果为 `"I"`、`"S"`\

对于toLowerCase(): 字符`"K"`经过toLowerCase处理后结果为`"k"`(这个K不是K)

## 单引号
```
%EF%BC%87
```

![](attachments/Pasted%20image%2020240403091813.png)